{
  "version": 2,
  "builds": [
    { "src": "api-vercel/**/*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "api-vercel/$1" },
    { "src": "/health", "dest": "api-vercel/health.js" }
  ]
}
